Table from myra-agents. Use via `window.MyraUI.Table` (bundle loaded from the root `_ds_bundle.js`).

## Related

`TableBody`, `TableCaption`, `TableCell`, `TableFooter`, `TableHead`, `TableHeader`, `TableRow`
