Card from myra-agents. Use via `window.MyraUI.Card` (bundle loaded from the root `_ds_bundle.js`).

## Related

`CardAction`, `CardContent`, `CardDescription`, `CardFooter`, `CardHeader`, `CardTitle`
