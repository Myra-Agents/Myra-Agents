Sheet from myra-agents. Use via `window.MyraUI.Sheet` (bundle loaded from the root `_ds_bundle.js`).

## Related

`SheetClose`, `SheetContent`, `SheetDescription`, `SheetFooter`, `SheetHeader`, `SheetTitle`, `SheetTrigger`
