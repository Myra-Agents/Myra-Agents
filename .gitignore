.claude/worktrees
# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo

# tauri
/src-tauri/target
# compiled sidecar binaries (built per-platform via `bun run sidecar:build`)
/src-tauri/binaries
# bun build --compile scratch files
.*.bun-build
.claude/scheduled_tasks.lock

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env
.env.clerk
.env.*

# vercel
.vercel
.claude/skills/validate-tauri-app/artifacts/
# exported signing identity (private key) — never commit
sign/*.p12
# working App Store metadata copy (asc-metadata.example.json is the template)
scripts/asc-metadata.json
# design exports
.figma-assets/
/docs/design-mockups/


# test artifacts
.playwright-cli
next-env.d.ts

# build-in-public drafts (changelog-based social media posts)
/docs/build-in-public/

# design-sync (claude.ai/design)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/ds-compiled.css
.design-sync/node_modules/
.design-sync/pkg/
