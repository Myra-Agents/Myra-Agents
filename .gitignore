# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo

# tauri
/src-tauri/target
# compiled sidecar binaries (built per-platform via `bun run sidecar:build`)
/src-tauri/binaries
# bun build --compile scratch files
.*.bun-build
.claude/scheduled_tasks.lock

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env
.env.clerk
.env.*

# vercel
.vercel
node_modules
.claude/skills/validate-tauri-app/artifacts/
# exported signing identity (private key) — never commit
sign/*.p12
